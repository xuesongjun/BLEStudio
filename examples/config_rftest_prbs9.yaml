# BLE Studio - RF Test PRBS9

# 公共参数
common:
  mode: "rf_test"
  channel: 0  # 2402 MHz

# 发送端
tx:
  phy_mode: "LE_1M"
  sample_rate: 8.0e6
  modulation_index: 0.5
  bt: 0.5
  payload_type: "PRBS9"
  payload_length: 37
  whitening: false

# 信道模型 (理想)
channel:
  snr_db: 30
  freq_offset: 0

# 输出
output:
  dir: "results"
  html_report: true
