# BLE Studio - 低 SNR 灵敏度测试

# 公共参数
common:
  mode: "advertising"
  channel: 38

# 发送端
tx:
  phy_mode: "LE_1M"
  sample_rate: 8.0e6
  modulation_index: 0.5
  bt: 0.5
  adv_address: "AA:BB:CC:DD:EE:FF"
  adv_data: "0201060908424c455374756469"

# 信道模型 (低 SNR + 频偏)
channel:
  snr_db: 10
  freq_offset: 100.0e3

# 输出
output:
  dir: "results"
  html_report: true
